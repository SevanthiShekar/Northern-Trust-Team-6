# copy to .env and fill in real values
MONGO_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/Payment_Gateway_Stimulation?retryWrites=true&w=majority
PORT=3000

# optional webhook URL. if you set this the server will POST JSON to it
# whenever a payment status changes.
# e.g. WEBHOOK_URL=https://webhook.site/your‑unique‑id
WEBHOOK_URL=
